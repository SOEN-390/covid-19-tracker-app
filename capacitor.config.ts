import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.covid19-tracker.Team01',
	appName: 'COVID-19 Tracker',
	webDir: 'build',
	bundledWebRuntime: false
};

export default config;
