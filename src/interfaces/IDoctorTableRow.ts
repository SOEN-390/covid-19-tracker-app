export interface IDoctorTableRow {
	firstName: string,
	lastName: string,
	licenseId: string,
	phoneNumber: string,
	address: Date;
	email: string;
	emergencyLeave: boolean;
	assignedPatientsCount: string;
}
